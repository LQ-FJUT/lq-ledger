# L.Q记账：项目介绍与使用指南

## 项目作用

L.Q记账是一款完全离线的 Windows 个人消费账本。它专注人民币支出记录，支持快速记账、分类管理、搜索筛选、月度统计、预算、软删除、完整 JSON 备份恢复和 CSV 报表，不需要账号或网络服务。

当前发布版本为 **v1.3.2**。

## 安装

1. 在 GitHub 仓库打开 **Releases**。
2. 下载 `L.Q记账_1.3.2_x64-setup.exe`。
3. 运行安装程序并按提示完成安装。

安装包未签名，Windows 可能显示未知发布者。请先核对 Release 页面的 SHA-256。

## 基本使用

1. 在记账页填写金额、分类、地点和备注，保存后可继续连续记账。
2. 使用最近分类、复制上一笔、金额算式和常用模板缩短录入步骤。
3. 在记录页按月份、关键词、分类、日期或金额筛选，并可恢复软删除记录。
4. 在统计页查看月度分类、上月对比、日均、最高消费日、六个月趋势和预算。
5. 在设置中调整分类、模板、地点、主题与界面密度。
6. 在备份与恢复中定期导出完整 JSON；CSV 仅用于 Excel 查看，不能恢复账本。

## 数据与隐私

- 账本使用本机 SQLite 保存，应用日常运行不联网。
- 恢复外部 JSON 前会校验数据并自动备份当前账本。
- 可选择每日滚动备份目录；正常卸载不主动删除应用数据。
- 跨电脑迁移应使用完整 JSON，不要复制安装目录或构建目录。

## 从源码运行

需要 Node.js、Rust 工具链和 Windows 构建环境。

```powershell
npm install
npm test
npm run build
npm run desktop:dev
```

构建 Windows 安装包：

```powershell
npm run desktop:build
```

## 发布状态

本仓库保存 v1.3.2 最终源码，安装包通过 GitHub Release 分发。本地测试、构建与隔离 smoke 不等同于干净 Windows 首装、升级、卸载后数据保留、离线启动和 WebView2 缺失路径验收。
